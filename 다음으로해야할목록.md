# 다음으로 해야할 목록

이 문서는 현재 mini-redis 프로젝트를

- MongoDB 전용 연동 구조에서
- 여러 외부 DB를 사용자가 설정으로 선택할 수 있는 구조로 바꾸고
- 동작 방식도 write-through 성격에서 cache-aside 패턴 중심으로 바꾸기 위해

어떻게 작업하면 되는지를 상세하게 정리한 계획서입니다.

## 1. 먼저 결론

결론부터 말하면 **가능합니다.**

하지만 정확히 말하면:

- "아무 DB나 자동으로 다 호환"은 어렵고
- "지원하는 DB 어댑터를 계속 추가할 수 있는 구조"로 만드는 것은 가능합니다

즉 목표는 이렇게 잡는 것이 현실적입니다.

### 목표

- MongoDB 전용 코드를 일반화한다
- 외부 원본 DB를 추상화한다
- 사용자가 설정으로 원본 DB 종류를 고를 수 있게 한다
- Redis 메모리 저장소는 캐시 계층으로 사용한다
- 읽기 전략은 cache-aside 패턴으로 바꾼다

이렇게 하면 나중에 아래 같은 DB를 추가할 수 있습니다.

- MongoDB
- PostgreSQL
- MySQL
- SQLite
- 파일 기반 원본 저장소
- 사내 전용 API/스토리지 어댑터

## 2. 지금 구조에서 바로 보이는 핵심 문제

현재 코드는 MongoDB가 "선택적 외부 DB"이긴 하지만, 구조적으로는 **Mongo 전용 결합**이 아직 남아 있습니다.

대표적으로:

- `bootstrap.py`에서 `MongoAdapter`, `MongoManager`를 직접 생성함
- `redis.py`가 `MongoManager`를 직접 받음
- `INFO MONGO`, `BENCHMARK MONGO`, `HYBRID`처럼 Mongo 이름이 명령/응답에 직접 들어가 있음
- 외부 원본 저장소 인터페이스가 아니라 Mongo 메서드 이름에 맞춰 설계되어 있음

즉 지금 구조는:

- "외부 원본 저장소" 추상화

가 아니라

- "Mongo를 붙일 수 있는 Mini Redis"

에 가깝습니다.

## 3. 바꾸고 싶은 최종 구조

목표 구조는 아래처럼 잡는 것이 좋습니다.

```text
CLI
-> TCP / RESP
-> CommandManager
-> handlers
-> CacheService (or Redis engine)
-> Cache storage (current StorageManager)
-> Source adapter (Mongo / Postgres / MySQL / SQLite ...)
```

여기서 개념은 이렇게 나뉩니다.

- Cache storage: 지금의 `StorageManager`
- Source adapter: 실제 원본 DB
- Service layer: cache-aside 읽기/쓰기 정책 담당

즉 핵심은 **MongoManager를 일반 SourceManager/SourceAdapter 개념으로 바꾸는 것**입니다.

## 4. cache-aside 패턴으로 바꾸면 어떻게 달라지는가

현재 구조는 대체로:

```text
SET
-> Redis memory write
-> Mongo sync
```

에 가깝습니다.

하지만 cache-aside 구조는 읽기에서 특히 다릅니다.

### GET 흐름

```text
GET key
-> cache 먼저 조회
-> 있으면 즉시 반환
-> 없으면 source DB 조회
-> source 값이 있으면 cache 저장
-> 반환
```

### SET 흐름

실무에서는 보통 아래 둘 중 하나를 씁니다.

#### 방법 A. source write 후 cache update

```text
SET key value
-> source DB 저장
-> cache 갱신
-> 반환
```

#### 방법 B. source write 후 cache invalidate

```text
SET key value
-> source DB 저장
-> cache 삭제
-> 다음 GET에서 다시 cache 채움
```

처음 구현은 **방법 A**가 이해하기 쉽습니다.

### DELETE 흐름

```text
DELETE key
-> source DB 삭제
-> cache 삭제
-> 반환
```

즉 기준이 되는 저장소는 외부 DB이고, mini-redis 메모리 저장소는 캐시가 됩니다.

## 5. 설계 원칙

이 작업을 할 때는 아래 원칙을 지키는 것이 좋습니다.

### 원칙 1. Mongo 전용 이름을 일반화

예:

- `MongoAdapter` -> `SourceAdapter` 또는 `BackendAdapter`
- `MongoManager` -> `SourceManager` 또는 `BackendManager`

### 원칙 2. Redis 엔진이 특정 DB 이름을 알지 않게 만들기

지금은 `Redis`가 Mongo를 직접 압니다.

목표는:

- `Redis`는 "원본 저장소 인터페이스"만 알아야 함
- 그 뒤가 Mongo인지 PostgreSQL인지 몰라도 되게 만들기

### 원칙 3. cache 정책과 source 접근 정책 분리

한 클래스가 다 하면 나중에 유지보수가 어려워집니다.

즉:

- cache 조회/저장
- source 조회/저장
- cache-aside 정책

을 분리하는 것이 좋습니다.

### 원칙 4. 처음부터 모든 DB를 구현하지 않기

가장 위험한 방식은 처음부터 Mongo, Postgres, MySQL을 다 붙이는 것입니다.

좋은 방식은:

1. 공통 인터페이스 설계
2. Mongo adapter를 첫 구현체로 이식
3. 테스트 통과
4. SQLite 같은 두 번째 구현체 추가

입니다.

## 6. 추천하는 작업 순서

아래 순서대로 가는 것을 추천합니다.

## 7. 1단계: 개념 정리와 이름 재설계

가장 먼저 해야 할 일은 "Mongo를 일반 source로 바꾸는 이름 정리"입니다.

### 해야 할 일

- `MongoAdapter` 역할을 일반 인터페이스로 추상화
- `MongoManager` 역할을 일반 source manager로 추상화
- 설정 키도 Mongo 전용과 일반 source 전용을 나눠 설계

### 추천 새 개념

```text
SourceAdapter
SourceManager
CachePolicy
CacheService
```

### 왜 먼저 해야 하나

이 작업을 먼저 하지 않으면:

- 나중에 PostgreSQL 추가 시 이름이 전부 어색해지고
- 코드가 "사실상 Mongo 특화" 상태로 계속 남습니다

## 8. 2단계: 공통 인터페이스 정의

이 단계가 가장 중요합니다.

예를 들어 아래 같은 인터페이스를 먼저 생각할 수 있습니다.

```python
class SourceAdapter(Protocol):
    enabled: bool

    def get(self, key: str) -> str | None: ...
    def set(self, key: str, value: str) -> None: ...
    def delete(self, key: str) -> None: ...
    def clear(self) -> None: ...
    def info(self) -> dict[str, Any]: ...
```

### 주의할 점

여기서는 Mongo에만 있는 개념을 넣지 않는 것이 중요합니다.

예:

- `collection`
- `document`
- `upsert`

같은 표현은 인터페이스 바깥 구현체에서만 쓰는 것이 좋습니다.

인터페이스는 최대한 일반적이어야 합니다.

## 9. 3단계: Mongo 구현체를 첫 번째 SourceAdapter로 이식

이 단계에서는 기존 Mongo 코드를 버리지 않고 **새 인터페이스 밑으로 옮깁니다.**

### 해야 할 일

- 기존 `MongoAdapter`를 `SourceAdapter` 구현체로 맞춤
- 기존 `MongoManager`를 `SourceManager` 혹은 구현체 전용 래퍼로 축소
- `write_value()` 같은 이름을 일반화

예:

- `write_value()` -> `set_value()`
- `read_value()` -> `get_value()`

### 목표

코드상에서 Mongo는:

- 특별한 존재가 아니라
- "첫 번째 source backend"

가 되게 만드는 것입니다.

## 10. 4단계: Redis engine에서 Mongo 직접 의존 제거

현재 [redis.py](D:/jungleCamp/Projects/miniRedis/src/mini_redis/engine/redis.py)는 `MongoManager`를 직접 받습니다.

이걸 바꿔야 진짜 확장이 가능합니다.

### 해야 할 일

- `Redis.__init__()`에서 `mongo` 대신 일반 source 인터페이스를 받게 변경
- `INFO MONGO` 같은 Mongo 전용 경로를 일반 정보 명령으로 바꾸기
- `BENCHMARK MONGO`, `HYBRID`도 재설계

### 바람직한 방향

```text
Redis
-> source.get()
-> source.set()
-> source.delete()
```

정도로만 알게 만들기

## 11. 5단계: cache-aside 전용 서비스 계층 추가

이 단계가 구조적으로 매우 중요합니다.

지금 `Redis`는:

- 캐시 역할
- 원본 저장 역할 일부
- persistence
- ttl
- invalidation

이 섞여 있습니다.

cache-aside로 가면, 아래 같은 전용 계층을 두는 것이 좋습니다.

### 예시 개념

```text
CacheAsideService
```

이 서비스의 책임:

- GET: cache hit/miss 처리
- SET: source 저장 후 cache 반영
- DELETE: source 삭제 후 cache 삭제
- TTL은 cache에만 적용할지 정책 결정
- invalidation 태그 처리

### 장점

- cache 전략이 명확해짐
- source backend 교체가 쉬워짐
- 테스트가 쉬워짐

## 12. 6단계: 설정 구조 재설계

사용자가 "어떤 외부 DB를 쓸지" 설정으로 선택하게 하려면 config 구조를 바꿔야 합니다.

현재는:

- `MINI_REDIS_MONGO_ENABLED`
- `MINI_REDIS_MONGO_URI`
- ...

처럼 Mongo 전용입니다.

### 추천 방향

```text
MINI_REDIS_SOURCE_BACKEND=mongo
MINI_REDIS_SOURCE_MONGO_URI=...
MINI_REDIS_SOURCE_MONGO_DB=...
MINI_REDIS_SOURCE_MONGO_COLLECTION=...
```

나중에 PostgreSQL이 추가되면:

```text
MINI_REDIS_SOURCE_BACKEND=postgres
MINI_REDIS_SOURCE_POSTGRES_DSN=...
```

같이 확장할 수 있습니다.

### 핵심

- 공통 선택 키 1개
- 구현체별 세부 설정 키 여러 개

이 구조가 가장 깔끔합니다.

## 13. 7단계: bootstrap을 factory 구조로 변경

현재 [bootstrap.py](D:/jungleCamp/Projects/miniRedis/src/mini_redis/bootstrap.py)는 Mongo를 직접 만듭니다.

이걸 아래 구조로 바꾸는 것이 좋습니다.

```text
build_source_adapter(config)
-> if mongo: MongoSourceAdapter(...)
-> if sqlite: SQLiteSourceAdapter(...)
-> if postgres: PostgresSourceAdapter(...)
```

즉 `bootstrap.py`는 특정 DB 클래스 이름을 너무 많이 알지 않게 하고,  
source factory를 통해 구현체를 선택하게 만드는 것이 좋습니다.

## 14. 8단계: 명령 설계 재정리

cache-aside 구조로 가면 일부 명령은 의미가 달라집니다.

### 다시 생각해야 할 명령

- `GET`
- `MGET`
- `SET`
- `DELETE`
- `EXPIRE`
- `TTL`
- `FLUSHDB`
- `INFO`
- `BENCHMARK`

### 특히 중요한 질문

- `FLUSHDB`는 cache만 비울 것인가?
- source DB도 같이 비울 것인가?
- `TTL`은 cache TTL인가, source 데이터 TTL인가?
- `SAVE/LOAD/AOF/RDB`는 cache에 계속 필요할까?

이 질문을 먼저 정하지 않으면 구현 중간에 계속 흔들립니다.

## 15. 9단계: persistence 역할 재정의

이건 꽤 큰 결정입니다.

현재 persistence는 Redis 메모리 상태를 저장합니다.

그런데 cache-aside 구조에서는 cache가 날아가도 source에서 다시 채울 수 있습니다.

그래서 선택지는 크게 세 가지입니다.

### 선택지 A. cache persistence 유지

- 캐시 재시작 후 warm cache를 빨리 복구 가능
- 구조는 복잡해짐

### 선택지 B. cache persistence 축소

- 캐시는 날아가도 다시 채우면 됨
- 구조가 단순해짐

### 선택지 C. cache persistence 옵션화

- 사용자가 켜고 끌 수 있게 함

처음에는 **옵션화**가 가장 현실적입니다.

## 16. 10단계: 새로운 테스트 전략 수립

이 구조 변경은 테스트 없이는 위험합니다.

반드시 필요한 테스트:

### cache-aside 동작 테스트

- cache hit면 source를 안 읽는지
- cache miss면 source를 읽는지
- miss 후 cache가 채워지는지

### source write 후 cache 반영 테스트

- SET 후 source 저장 성공 여부
- SET 후 cache update 또는 invalidate 여부
- DELETE 후 source/cache 동시 반영 여부

### backend 선택 테스트

- 설정이 mongo일 때 mongo adapter 선택
- 설정이 sqlite일 때 sqlite adapter 선택
- 잘못된 설정이면 적절한 에러 반환

### 장애 테스트

- source DB가 죽어 있을 때 GET/SET 동작
- cache는 있는데 source가 죽어 있을 때 정책 확인
- source write 실패 시 cache 오염 방지

## 17. 11단계: 관측성 강화

운영에서 cache-aside는 "왜 miss가 났는지"를 보는 것이 매우 중요합니다.

추가하면 좋은 관측 항목:

- cache hits
- cache misses
- source reads
- source writes
- source read latency
- source write latency
- backend type
- cache fill count
- invalidate count

즉 `INFO`에 아래 같은 정보가 들어가면 좋습니다.

```text
cache.hit_count
cache.miss_count
source.backend
source.read_count
source.write_count
```

## 18. 12단계: 두 번째 구현체 추가

Mongo만 지원하면 구조가 진짜 일반화되었는지 알기 어렵습니다.

그래서 첫 번째 일반화 후에는 두 번째 구현체를 꼭 하나 넣는 것이 좋습니다.

### 추천 두 번째 구현체

- SQLite

이유:

- 로컬 테스트 쉬움
- 설정 간단함
- 개발 속도 빠름
- PostgreSQL/MySQL보다 붙이기 쉬움

즉:

1. Mongo 일반화
2. SQLite 추가

까지 해보면 구조가 제대로 되었는지 금방 알 수 있습니다.

## 19. 상세 작업 순서

아래 순서대로 실제 작업하는 것을 추천합니다.

### 1. 문서와 목표 확정

- source of truth를 외부 DB로 정한다
- cache-aside 정책을 명시한다
- cache TTL과 source TTL을 구분한다

### 2. 인터페이스 추가

- `SourceAdapter` 프로토콜 또는 추상 클래스 생성
- 공통 메서드 정의

### 3. Mongo 코드를 새 인터페이스 아래로 이동

- Mongo 구현체를 `SourceAdapter`에 맞춤
- Mongo-specific manager를 최소화

### 4. config 일반화

- `SOURCE_BACKEND` 개념 도입
- backend별 설정 키 분리

### 5. bootstrap factory 도입

- 설정에 따라 backend adapter 생성

### 6. engine 또는 service layer 재구성

- `get()`을 cache-aside 방식으로 변경
- `set()`/`delete()`를 source 중심으로 변경

### 7. command 의미 재정의

- `INFO`
- `BENCHMARK`
- `FLUSHDB`
- `TTL`

정리

### 8. 테스트 작성

- cache hit/miss
- source fallback
- backend selection
- source failure

### 9. observability 추가

- hit/miss 카운터
- source latency
- backend 정보

### 10. 두 번째 backend 추가

- SQLite 추천

## 20. 파일 단위로 보면 어디를 건드리게 될까

예상 변경 파일:

- [config.py](D:/jungleCamp/Projects/miniRedis/src/mini_redis/config.py)
- [bootstrap.py](D:/jungleCamp/Projects/miniRedis/src/mini_redis/bootstrap.py)
- [redis.py](D:/jungleCamp/Projects/miniRedis/src/mini_redis/engine/redis.py)
- [mongo_adapter.py](D:/jungleCamp/Projects/miniRedis/src/mini_redis/storage/mongo_adapter.py)
- [mongo_manager.py](D:/jungleCamp/Projects/miniRedis/src/mini_redis/storage/mongo_manager.py)
- [benchmark.py](D:/jungleCamp/Projects/miniRedis/src/mini_redis/storage/benchmark.py)
- [info.py](D:/jungleCamp/Projects/miniRedis/src/mini_redis/commands/handlers/info.py)
- [config.py](D:/jungleCamp/Projects/miniRedis/src/mini_redis/commands/handlers/config.py)
- 테스트 파일들

추가될 가능성이 큰 새 파일:

- `src/mini_redis/storage/source_adapter.py`
- `src/mini_redis/storage/source_factory.py`
- `src/mini_redis/storage/sqlite_adapter.py`
- `src/mini_redis/engine/cache_service.py`

## 21. 위험한 지점

이 작업에는 몇 가지 숨은 위험이 있습니다.

### 위험 1. 기존 persistence 의미가 애매해짐

cache 서버인데 AOF/RDB를 계속 강하게 유지할지 결정이 필요합니다.

### 위험 2. TTL 의미 충돌

cache TTL과 source 데이터 수명을 헷갈릴 수 있습니다.

### 위험 3. invalidation 의미 변화

지금은 메모리 저장소 기준 invalidation인데, source 중심 구조에선 cache invalidation 의미가 더 강해집니다.

### 위험 4. 명령 이름은 같은데 내부 의미가 바뀜

사용자는 `SET`, `GET`가 똑같아 보여도 내부 정책은 많이 달라집니다.

그래서 문서 업데이트가 꼭 필요합니다.

## 22. 현실적인 첫 구현 범위

처음부터 크게 가지 말고 아래 범위로 자르는 것을 추천합니다.

### 1차 목표

- backend 선택 지원
- `mongo`와 `sqlite` 두 가지 지원
- `GET`, `SET`, `DELETE`, `MGET`만 cache-aside로 전환
- `INFO SOURCE` 같은 일반 정보 명령 추가

### 2차 목표

- TTL 정책 정리
- invalidation 연동
- benchmark 일반화

### 3차 목표

- persistence 옵션화
- 추가 backend 확장

## 23. 내가 추천하는 구현 방향

가장 추천하는 방향은 아래입니다.

### 추천 방향

1. Mongo 코드를 일반 source 인터페이스로 추상화
2. cache-aside 전용 service 계층 추가
3. SQLite를 두 번째 source로 붙여 구조 검증
4. 그 뒤에 PostgreSQL/MySQL 확장

이 순서가 좋은 이유:

- 현재 코드와 충돌이 적고
- 단계별 검증이 가능하고
- "진짜로 여러 DB를 지원하는 구조인지" 확인하기 쉽기 때문입니다

## 24. 최종 요약

질문에 대한 답은:

- **가능하다**
- 하지만 "Mongo 전용 연동"을 "일반 source backend 구조"로 먼저 바꿔야 한다
- 그리고 cache-aside는 읽기/쓰기/persistence/TTL 의미를 꽤 많이 바꾸는 작업이다

즉 다음 작업의 핵심은:

1. Mongo 결합 끊기
2. SourceAdapter 추상화 만들기
3. cache-aside 서비스 계층 도입하기
4. 설정 기반 backend 선택 구조 만들기

입니다.
