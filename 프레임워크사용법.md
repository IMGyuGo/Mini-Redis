# 프레임워크 사용법

이 문서는 현재 mini-redis 프로젝트를 앞으로

- 여러 외부 DB를 선택할 수 있는 구조로 만들고
- cache-aside 패턴 중심으로 바꿨다고 가정할 때

Spring이나 Flask 같은 애플리케이션 프레임워크에서 이 서버를 어떻게 연결해서 사용할 수 있는지 정리한 문서입니다.

## 1. 먼저 큰 그림

이 mini-redis 서버를 프레임워크에서 붙여 쓴다는 것은 보통 아래 의미입니다.

```text
Spring / Flask 앱
-> mini-redis 서버에 캐시 요청
-> cache miss면 원본 DB 조회
-> 조회 결과를 mini-redis에 저장
```

그런데 앞으로 프로젝트를 cache-aside 구조로 바꾼다면, 연결 방식은 크게 두 가지가 있습니다.

## 2. 두 가지 연결 방식

### 방식 A. 애플리케이션이 직접 cache-aside를 수행

이 방식은 Spring 또는 Flask 애플리케이션이 직접 아래 흐름을 처리합니다.

```text
1. mini-redis에서 먼저 조회
2. 없으면 원본 DB 조회
3. 원본 결과를 mini-redis에 저장
4. 반환
```

장점:

- 프레임워크 코드에서 정책을 세밀하게 제어하기 쉬움
- 서비스별로 다른 캐시 정책을 넣기 쉬움
- 가장 일반적인 실무 패턴과 비슷함

단점:

- 앱 코드에 캐시 로직이 반복될 수 있음

### 방식 B. mini-redis 서버가 cache-aside를 내부에서 수행

이 방식은 mini-redis가 source adapter까지 알고 있다고 가정하는 방식입니다.

```text
1. 앱은 그냥 GET/SET 요청만 보냄
2. mini-redis가 내부적으로 cache 조회
3. miss면 source DB 조회
4. cache 채우고 반환
```

장점:

- 앱이 단순해짐
- 캐시 정책을 서버 한 곳에서 통일 가능

단점:

- mini-redis 서버 역할이 커짐
- 프레임워크별 세밀한 비즈니스 캐시 정책을 넣기 어렵거나 복잡해짐

## 3. 어떤 방식을 추천하나

실무적으로는 **방식 A**, 즉 **애플리케이션이 직접 cache-aside를 수행하는 방식**을 먼저 추천합니다.

이유:

- 프레임워크 코드 안에서 비즈니스 기준으로 캐시 키를 만들기 쉬움
- 어떤 데이터를 캐시할지 더 명확함
- mini-redis 서버를 더 범용적으로 유지할 수 있음

즉 처음에는:

- mini-redis = 캐시 서버
- Spring / Flask 앱 = cache-aside 정책 담당

구조가 더 현실적입니다.

## 4. mini-redis를 프레임워크에서 사용할 때 기본 연결 개념

이 서버는 현재 TCP + RESP 기반입니다.

즉 프레임워크 입장에서는 다음 중 하나가 필요합니다.

- mini-redis 전용 클라이언트
- RESP를 사용할 수 있는 클라이언트
- HTTP gateway를 하나 더 두는 방식

가장 쉬운 현실적인 선택은 아래 둘 중 하나입니다.

### 선택 1. 전용 애플리케이션 클라이언트 작성

Spring/Flask용으로 아주 얇은 client wrapper를 만듭니다.

예:

- `get(key)`
- `set(key, value, ttl)`
- `delete(key)`

만 감싸는 래퍼

### 선택 2. mini-redis 앞에 HTTP API gateway 추가

예:

```text
Spring / Flask 앱
-> HTTP cache gateway
-> mini-redis TCP server
```

이 방식은 앱 입장에서 HTTP로만 호출하면 되기 때문에 붙이기 쉽습니다.

하지만 현재 프로젝트 기준으로는 우선 **전용 client wrapper 방식**이 더 자연스럽습니다.

## 5. Spring에서 사용하는 방법

Spring에서는 보통 아래 세 가지 수준으로 사용할 수 있습니다.

## 6. Spring 방식 1: Service 계층에서 직접 client 호출

가장 이해하기 쉽고 추천하기 쉬운 방식입니다.

### 구조

```text
Controller
-> Service
-> MiniRedisClient
-> miss면 Repository
-> 결과 캐시
```

### 예시 흐름

예를 들어 사용자 조회 서비스:

1. `UserService.getUser(id)` 호출
2. `miniRedisClient.get("user:" + id)` 조회
3. 있으면 바로 반환
4. 없으면 `userRepository.findById(id)` 조회
5. 조회 결과를 `miniRedisClient.set(...)` 저장
6. 반환

### 장점

- 이해하기 쉬움
- 명시적임
- 디버깅이 쉬움

### 어떤 경우에 좋나

- 프로젝트 초기
- 캐시 정책을 직접 통제하고 싶을 때
- 학습용 / 실험용

## 7. Spring 방식 2: CacheService 추상화 추가

규모가 조금 커지면 Service에서 매번 같은 캐시 로직을 쓰기 번거롭습니다.

그래서 아래처럼 감쌀 수 있습니다.

```text
UserService
-> CacheAsideHelper
-> MiniRedisClient + Repository
```

예를 들면:

- `cacheService.getOrLoad(key, ttl, loader)`

같은 형태입니다.

이렇게 하면 Service 코드가 훨씬 깔끔해집니다.

### 장점

- 중복 감소
- 캐시 정책 통일
- 키 생성 규칙 표준화 가능

## 8. Spring 방식 3: Spring Cache처럼 감싸기

더 발전시키면 mini-redis 전용 캐시 구현체를 만들어 Spring Cache abstraction과 연결할 수도 있습니다.

하지만 이건 초기 단계에서는 추천하지 않습니다.

이유:

- 구현량이 커짐
- 현재 mini-redis 명령/프로토콜과 매핑해야 할 것이 많음
- 먼저 직접 client + helper 방식으로 검증하는 편이 안전함

즉 초기 추천 순서는:

1. Service에서 직접 사용
2. Cache helper 추가
3. 필요하면 Spring Cache 통합

## 9. Spring에서 실제로는 어디에 붙이는가

가장 보편적인 위치는 Service 계층입니다.

### 추천 위치

- Controller에서 직접 캐시 접근하지 않기
- Repository 안에 캐시 로직 넣지 않기
- Service 계층에서 캐시 + DB 흐름을 조절하기

즉:

```text
Controller
-> Service  # cache policy
-> Repository  # source DB access
```

이 구조가 가장 깔끔합니다.

## 10. Spring 예시 구조

```text
controller/
service/
repository/
cache/
  MiniRedisClient.java
  CacheAsideHelper.java
```

### 추천 클래스 예시

- `MiniRedisClient`
- `CacheKeyBuilder`
- `CacheAsideHelper`
- `UserCacheService` 또는 `ProductCacheService`

## 11. Flask에서 사용하는 방법

Flask도 원리는 똑같습니다.

다만 보통은 Java/Spring보다 더 단순하게 시작할 수 있습니다.

## 12. Flask 방식 1: route/service 함수에서 직접 사용

예를 들어:

```text
Flask route
-> service function
-> mini redis client
-> miss면 DB 조회
-> cache 저장
```

### 예시 흐름

1. `/users/<id>` 요청
2. `mini_redis_client.get(f"user:{id}")`
3. 있으면 반환
4. 없으면 SQLAlchemy 또는 Mongo client로 원본 조회
5. 결과를 캐시에 저장
6. 반환

### 장점

- 가장 단순함
- 빠르게 붙일 수 있음

## 13. Flask 방식 2: helper 함수로 공통화

Flask도 결국 같은 cache-aside 코드가 반복됩니다.

그래서 아래처럼 공통 함수로 빼는 것이 좋습니다.

- `get_or_load(key, ttl, loader)`
- `invalidate_by_key(key)`
- `invalidate_by_tag(tag)`

이 helper를 service 레이어나 util 모듈로 두면 코드가 깔끔해집니다.

## 14. Flask 방식 3: extension처럼 감싸기

더 나아가면 Flask extension처럼 만들 수도 있습니다.

예:

- `mini_redis_ext = MiniRedisExtension(app)`

하지만 이것도 초반에는 오버엔지니어링일 수 있으므로, 먼저 가벼운 client wrapper부터 시작하는 편이 좋습니다.

## 15. Flask에서 어디에 붙이는가

Flask에서는 보통 아래 위치가 적절합니다.

- `services/`
- `cache/`
- `utils/`

추천 구조:

```text
app/
  routes/
  services/
  repositories/
  cache/
    client.py
    helper.py
```

## 16. Spring과 Flask 모두에서 중요한 공통 원칙

프레임워크가 무엇이든 아래 원칙은 매우 중요합니다.

### 원칙 1. 캐시 키 규칙을 먼저 정하기

예:

- `user:{id}`
- `product:{id}`
- `user:{id}:profile`

키 규칙이 없으면 운영하면서 혼란이 생깁니다.

### 원칙 2. TTL 정책을 데이터 종류별로 정하기

예:

- 사용자 프로필: 60초
- 게시글 목록: 30초
- 설정값: 300초

### 원칙 3. source DB 조회 함수와 캐시 로직을 분리하기

캐시 miss가 났을 때 호출하는 "원본 조회 함수"는 명확해야 합니다.

### 원칙 4. 실패 정책을 정하기

예:

- mini-redis가 죽으면 DB만 보고 계속 서비스할 것인가?
- mini-redis SET 실패는 무시할 것인가?
- source DB 실패 시 stale cache를 허용할 것인가?

이런 정책이 꼭 필요합니다.

## 17. 실무적으로 가장 추천하는 연결 방식

가장 현실적인 추천은 아래입니다.

### Spring

```text
Controller
-> Service
-> MiniRedisClient
-> miss면 Repository
-> cache set
```

### Flask

```text
Route
-> Service function
-> MiniRedisClient
-> miss면 Repository/DB access
-> cache set
```

즉 둘 다 핵심은 같습니다.

- cache는 앱 서비스 레이어에서 먼저 확인
- miss면 원본 DB 조회
- 조회 결과를 mini-redis에 캐시

## 18. 이 mini-redis 프로젝트를 framework-friendly하게 만들려면 추가로 필요한 것

프레임워크에서 더 쓰기 좋게 만들려면 mini-redis 쪽에도 보완이 필요합니다.

### 추가하면 좋은 것

- 더 명확한 RESP/명령 호환성
- 안정적인 `GET`, `SET`, `DELETE`, `MGET`
- TTL 명령 안정화
- `INFO SOURCE`, `INFO CACHE` 같은 관측 명령
- key prefix / namespace 관리
- 에러 메시지 표준화
- timeout 처리
- connection 안정성

그리고 가능하면 나중에 아래도 있으면 좋습니다.

- 공식 Python client
- 공식 Java client
- 최소한의 사용 예제

## 19. 만약 mini-redis가 내부적으로 source DB까지 아는 구조라면?

그 경우 Spring/Flask는 더 단순해질 수 있습니다.

예:

```text
앱
-> mini-redis GET
-> mini-redis가 cache miss면 source DB 조회까지 수행
-> 앱은 결과만 받음
```

하지만 이 방식은 주의할 점이 있습니다.

- 앱이 캐시 정책을 세밀하게 제어하기 어려움
- source DB 의존이 mini-redis 서버에 몰림
- 비즈니스별 캐시 정책이 분산되지 않음

그래서 처음에는 앱 레이어에서 cache-aside를 수행하는 구조를 더 추천합니다.

## 20. 어떤 방식으로 시작하는 것이 가장 좋은가

가장 추천하는 시작 순서는 아래입니다.

### 1단계

- mini-redis를 단순 캐시 서버로 사용
- Spring/Flask에서 직접 cache-aside 구현

### 2단계

- 앱 안에 `CacheAsideHelper` 추가
- 키 규칙, TTL 규칙 표준화

### 3단계

- mini-redis에 source backend 선택 기능 추가
- 필요하면 mini-redis 내부 cache-aside 모드 실험

즉 처음부터 mini-redis 안에 모든 정책을 몰아넣기보다,  
애플리케이션 쪽에서 먼저 사용 패턴을 검증하는 것이 더 안전합니다.

## 21. 정리

Spring이든 Flask든 연결 방법의 핵심은 같습니다.

- mini-redis를 캐시 서버로 둔다
- 앱 서비스 계층에서 먼저 캐시를 조회한다
- miss면 원본 DB를 읽는다
- 읽은 결과를 다시 mini-redis에 저장한다

이 방식이 가장 이해하기 쉽고, 운영에서도 많이 쓰는 구조입니다.

## 22. 한 줄 요약

Spring과 Flask에서는 mini-redis를 보통 **서비스 계층에서 사용하는 캐시 서버**로 연결하고,  
가장 추천하는 방식은 애플리케이션이 직접 cache-aside 패턴을 수행하는 구조입니다.
