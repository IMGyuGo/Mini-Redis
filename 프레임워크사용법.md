# 프레임워크 사용법

이 문서는 현재 mini-redis 프로젝트를 앞으로

- 여러 외부 DB를 선택할 수 있는 구조로 만들고
- cache-aside 패턴 중심으로 바꿨다고 가정할 때

Spring이나 Flask 같은 애플리케이션 프레임워크에서 이 서버를 어떻게 연결해서 사용할 수 있는지 정리한 문서입니다.

## 1. 먼저 큰 그림

이 mini-redis 서버를 프레임워크에서 붙여 쓴다는 것은 보통 아래 의미입니다.

```text
Spring / Flask 앱
-> mini-redis 서버에 캐시 요청
-> cache miss면 원본 DB 조회
-> 조회 결과를 mini-redis에 저장
```

그런데 앞으로 프로젝트를 cache-aside 구조로 바꾼다면, 연결 방식은 크게 두 가지가 있습니다.

## 2. 두 가지 연결 방식

### 방식 A. 애플리케이션이 직접 cache-aside를 수행

이 방식은 Spring 또는 Flask 애플리케이션이 직접 아래 흐름을 처리합니다.

```text
1. mini-redis에서 먼저 조회
2. 없으면 원본 DB 조회
3. 원본 결과를 mini-redis에 저장
4. 반환
```

장점:

- 프레임워크 코드에서 정책을 세밀하게 제어하기 쉬움
- 서비스별로 다른 캐시 정책을 넣기 쉬움
- 가장 일반적인 실무 패턴과 비슷함

단점:

- 앱 코드에 캐시 로직이 반복될 수 있음

### 방식 B. mini-redis 서버가 cache-aside를 내부에서 수행

이 방식은 mini-redis가 source adapter까지 알고 있다고 가정하는 방식입니다.

```text
1. 앱은 그냥 GET/SET 요청만 보냄
2. mini-redis가 내부적으로 cache 조회
3. miss면 source DB 조회
4. cache 채우고 반환
```

장점:

- 앱이 단순해짐
- 캐시 정책을 서버 한 곳에서 통일 가능

단점:

- mini-redis 서버 역할이 커짐
- 프레임워크별 세밀한 비즈니스 캐시 정책을 넣기 어렵거나 복잡해짐

## 3. 어떤 방식을 추천하나

실무적으로는 **방식 A**, 즉 **애플리케이션이 직접 cache-aside를 수행하는 방식**을 먼저 추천합니다.

이유:

- 프레임워크 코드 안에서 비즈니스 기준으로 캐시 키를 만들기 쉬움
- 어떤 데이터를 캐시할지 더 명확함
- mini-redis 서버를 더 범용적으로 유지할 수 있음

즉 처음에는:

- mini-redis = 캐시 서버
- Spring / Flask 앱 = cache-aside 정책 담당

구조가 더 현실적입니다.

## 4. mini-redis를 프레임워크에서 사용할 때 기본 연결 개념

이 서버는 현재 TCP + RESP 기반입니다.

즉 프레임워크 입장에서는 다음 중 하나가 필요합니다.

- mini-redis 전용 클라이언트
- RESP를 사용할 수 있는 클라이언트
- HTTP gateway를 하나 더 두는 방식

가장 쉬운 현실적인 선택은 아래 둘 중 하나입니다.

### 선택 1. 전용 애플리케이션 클라이언트 작성

Spring/Flask용으로 아주 얇은 client wrapper를 만듭니다.

예:

- `get(key)`
- `set(key, value, ttl)`
- `delete(key)`

만 감싸는 래퍼

### 선택 2. mini-redis 앞에 HTTP API gateway 추가

예:

```text
Spring / Flask 앱
-> HTTP cache gateway
-> mini-redis TCP server
```

이 방식은 앱 입장에서 HTTP로만 호출하면 되기 때문에 붙이기 쉽습니다.

하지만 현재 프로젝트 기준으로는 우선 **전용 client wrapper 방식**이 더 자연스럽습니다.

## 5. Spring에서 사용하는 방법

Spring에서는 보통 아래 네 가지 수준으로 사용할 수 있습니다.

## 6. Spring 방식 1: Service 계층에서 직접 client 호출

가장 이해하기 쉽고 추천하기 쉬운 방식입니다.

### 구조

```text
Controller
-> Service
-> MiniRedisClient
-> miss면 Repository
-> 결과 캐시
```

### 예시 흐름

예를 들어 사용자 조회 서비스:

1. `UserService.getUser(id)` 호출
2. `miniRedisClient.get("user:" + id)` 조회
3. 있으면 바로 반환
4. 없으면 `userRepository.findById(id)` 조회
5. 조회 결과를 `miniRedisClient.set(...)` 저장
6. 반환

### 장점

- 이해하기 쉬움
- 명시적임
- 디버깅이 쉬움

### 어떤 경우에 좋나

- 프로젝트 초기
- 캐시 정책을 직접 통제하고 싶을 때
- 학습용 / 실험용

## 7. Spring 방식 2: CacheService 추상화 추가

규모가 조금 커지면 Service에서 매번 같은 캐시 로직을 쓰기 번거롭습니다.

그래서 아래처럼 감쌀 수 있습니다.

```text
UserService
-> CacheAsideHelper
-> MiniRedisClient + Repository
```

예를 들면:

- `cacheService.getOrLoad(key, ttl, loader)`

같은 형태입니다.

이렇게 하면 Service 코드가 훨씬 깔끔해집니다.

### 장점

- 중복 감소
- 캐시 정책 통일
- 키 생성 규칙 표준화 가능

## 8. Spring 방식 3: `@Cacheable`, `@CachePut`, `@CacheEvict`처럼 사용하기

이 방식은 질문하신 방향과 가장 가깝습니다.

핵심은:

- mini-redis 서버가 Spring annotation을 직접 이해하는 것이 아니라
- Spring 애플리케이션 안에서 mini-redis용 `CacheManager`와 `Cache` 구현체를 만드는 것

입니다.

즉 구조는 아래와 같습니다.

```text
@Cacheable / @CachePut / @CacheEvict
-> Spring Cache Abstraction
-> MiniRedisCacheManager
-> MiniRedisCache
-> MiniRedisClient
-> mini-redis server
```

### 왜 이 방식이 좋은가

- Spring 개발자는 익숙한 annotation을 그대로 사용할 수 있음
- 비즈니스 코드가 깔끔해짐
- cache-aside 동작을 Spring 표준 방식처럼 보이게 만들 수 있음

### 실제 의미

#### `@Cacheable`

의미:

- 먼저 mini-redis에서 조회
- 있으면 메서드를 실행하지 않고 캐시값 반환
- 없으면 메서드 실행
- 실행 결과를 mini-redis에 저장

즉 전형적인 cache-aside 읽기 패턴과 거의 같습니다.

#### `@CachePut`

의미:

- 메서드는 항상 실행
- 실행 결과를 mini-redis에 저장 또는 갱신

즉 source DB를 갱신한 뒤 cache를 update하는 흐름과 잘 맞습니다.

#### `@CacheEvict`

의미:

- 메서드 실행 후 또는 실행 전에 cache key 삭제

즉 source DB 변경 후 cache invalidation 흐름과 잘 맞습니다.

### 이 방식에서 mini-redis 쪽에 필요한 것

- 안정적인 `GET`
- 안정적인 `SET`
- 안정적인 `DELETE`
- key 기반 조회/삭제
- TTL 지원
- 명확한 에러 처리

### 이 방식에서 Spring 쪽에 필요한 것

- `MiniRedisClient`
- `MiniRedisCache`
- `MiniRedisCacheManager`
- 필요하면 `KeyGenerator`
- 필요하면 TTL 정책 매핑

### 중요한 점

현재 mini-redis가 "실제 Redis와 완전 호환"은 아니기 때문에,  
바로 Spring Data Redis에 붙이기보다 **custom CacheManager 구현**이 더 현실적입니다.

## 9. Spring 방식 4: 나중에 Redis 호환성을 더 높여 Spring Data Redis처럼 사용하기

장기적으로는 아래도 가능할 수 있습니다.

- RESP 호환성을 더 높이고
- Redis 명령 호환 범위를 넓히고
- Spring Data Redis가 기대하는 동작과 맞추는 방식

하지만 이건 난도가 높습니다.

왜냐하면 Spring Data Redis는 단순히 `GET`, `SET`만 보는 것이 아니라:

- 직렬화
- connection 관리
- Redis command 동작 일관성
- 예외 처리

같은 부분도 기대하기 때문입니다.

그래서 현실적인 순서는:

1. custom `CacheManager`
2. annotation 기반 사용 검증
3. 필요하면 호환성 확대

입니다.

## 10. Spring에서 실제로는 어디에 붙이는가

가장 보편적인 위치는 Service 계층입니다.

### 추천 위치

- Controller에서 직접 캐시 접근하지 않기
- Repository 안에 캐시 로직 넣지 않기
- Service 계층에서 캐시 + DB 흐름을 조절하기

즉:

```text
Controller
-> Service  # cache policy
-> Repository  # source DB access
```

이 구조가 가장 깔끔합니다.

## 11. Spring 예시 구조

```text
controller/
service/
repository/
cache/
  MiniRedisClient.java
  CacheAsideHelper.java
```

### 추천 클래스 예시

- `MiniRedisClient`
- `CacheKeyBuilder`
- `CacheAsideHelper`
- `MiniRedisCache`
- `MiniRedisCacheManager`
- `UserCacheService` 또는 `ProductCacheService`

### annotation 기반 사용 예시 개념

```text
@Cacheable(cacheNames = "users", key = "#id")
public UserDto getUser(Long id)

@CachePut(cacheNames = "users", key = "#result.id")
public UserDto updateUser(...)

@CacheEvict(cacheNames = "users", key = "#id")
public void deleteUser(Long id)
```

이때 실제 cache 저장소는 mini-redis가 됩니다.

## 12. Flask에서 사용하는 방법

Flask도 원리는 똑같습니다.

다만 보통은 Java/Spring보다 더 단순하게 시작할 수 있습니다.

## 13. Flask 방식 1: route/service 함수에서 직접 사용

예를 들어:

```text
Flask route
-> service function
-> mini redis client
-> miss면 DB 조회
-> cache 저장
```

### 예시 흐름

1. `/users/<id>` 요청
2. `mini_redis_client.get(f"user:{id}")`
3. 있으면 반환
4. 없으면 SQLAlchemy 또는 Mongo client로 원본 조회
5. 결과를 캐시에 저장
6. 반환

### 장점

- 가장 단순함
- 빠르게 붙일 수 있음

## 14. Flask 방식 2: helper 함수로 공통화

Flask도 결국 같은 cache-aside 코드가 반복됩니다.

그래서 아래처럼 공통 함수로 빼는 것이 좋습니다.

- `get_or_load(key, ttl, loader)`
- `invalidate_by_key(key)`
- `invalidate_by_tag(tag)`

이 helper를 service 레이어나 util 모듈로 두면 코드가 깔끔해집니다.

## 15. Flask 방식 3: extension처럼 감싸기

더 나아가면 Flask extension처럼 만들 수도 있습니다.

예:

- `mini_redis_ext = MiniRedisExtension(app)`

하지만 이것도 초반에는 오버엔지니어링일 수 있으므로, 먼저 가벼운 client wrapper부터 시작하는 편이 좋습니다.

## 16. Flask에서 어디에 붙이는가

Flask에서는 보통 아래 위치가 적절합니다.

- `services/`
- `cache/`
- `utils/`

추천 구조:

```text
app/
  routes/
  services/
  repositories/
  cache/
    client.py
    helper.py
```

## 17. Spring과 Flask 모두에서 중요한 공통 원칙

프레임워크가 무엇이든 아래 원칙은 매우 중요합니다.

### 원칙 1. 캐시 키 규칙을 먼저 정하기

예:

- `user:{id}`
- `product:{id}`
- `user:{id}:profile`

키 규칙이 없으면 운영하면서 혼란이 생깁니다.

### 원칙 2. TTL 정책을 데이터 종류별로 정하기

예:

- 사용자 프로필: 60초
- 게시글 목록: 30초
- 설정값: 300초

### 원칙 3. source DB 조회 함수와 캐시 로직을 분리하기

캐시 miss가 났을 때 호출하는 "원본 조회 함수"는 명확해야 합니다.

### 원칙 4. 실패 정책을 정하기

예:

- mini-redis가 죽으면 DB만 보고 계속 서비스할 것인가?
- mini-redis SET 실패는 무시할 것인가?
- source DB 실패 시 stale cache를 허용할 것인가?

이런 정책이 꼭 필요합니다.

## 18. 실무적으로 가장 추천하는 연결 방식

가장 현실적인 추천은 아래입니다.

### Spring

```text
Controller
-> Service
-> Spring Cache Abstraction
-> MiniRedisCacheManager / MiniRedisCache
-> miss면 Repository
-> cache set
```

### Flask

```text
Route
-> Service function
-> MiniRedisClient
-> miss면 Repository/DB access
-> cache set
```

즉 둘 다 핵심은 같습니다.

- cache는 앱 서비스 레이어에서 먼저 확인
- miss면 원본 DB 조회
- 조회 결과를 mini-redis에 캐시

### Spring에서는 이렇게 해석하면 됩니다

- `@Cacheable` = cache miss 시 source DB를 읽고 결과 캐시
- `@CachePut` = source 변경 후 cache 갱신
- `@CacheEvict` = source 변경 후 cache 제거

즉 annotation 사용 경험은 Redis를 쓸 때와 비슷하게 만들 수 있습니다.

## 19. 이 mini-redis 프로젝트를 framework-friendly하게 만들려면 추가로 필요한 것

프레임워크에서 더 쓰기 좋게 만들려면 mini-redis 쪽에도 보완이 필요합니다.

### 추가하면 좋은 것

- 더 명확한 RESP/명령 호환성
- 안정적인 `GET`, `SET`, `DELETE`, `MGET`
- TTL 명령 안정화
- `INFO SOURCE`, `INFO CACHE` 같은 관측 명령
- key prefix / namespace 관리
- 에러 메시지 표준화
- timeout 처리
- connection 안정성

그리고 가능하면 나중에 아래도 있으면 좋습니다.

- 공식 Python client
- 공식 Java client
- 최소한의 사용 예제

### Spring annotation 친화적으로 만들기 위해 추가하면 특히 좋은 것

- 캐시 이름별 namespace 규칙
- key generator 규칙 문서화
- TTL을 cache name별로 다르게 줄 수 있는 정책
- null value 처리 정책
- serialization 포맷 고정
- `INFO CACHE` 같은 관측 명령

## 20. 만약 mini-redis가 내부적으로 source DB까지 아는 구조라면?

그 경우 Spring/Flask는 더 단순해질 수 있습니다.

예:

```text
앱
-> mini-redis GET
-> mini-redis가 cache miss면 source DB 조회까지 수행
-> 앱은 결과만 받음
```

하지만 이 방식은 주의할 점이 있습니다.

- 앱이 캐시 정책을 세밀하게 제어하기 어려움
- source DB 의존이 mini-redis 서버에 몰림
- 비즈니스별 캐시 정책이 분산되지 않음

그래서 처음에는 앱 레이어에서 cache-aside를 수행하는 구조를 더 추천합니다.

## 21. 어떤 방식으로 시작하는 것이 가장 좋은가

가장 추천하는 시작 순서는 아래입니다.

### 1단계

- mini-redis를 단순 캐시 서버로 사용
- Spring에서는 `MiniRedisCacheManager`를 통해 `@Cacheable` 스타일 연결
- Flask에서는 helper 방식으로 시작

### 2단계

- Spring annotation 동작을 검증
- 키 규칙, TTL 규칙, null 처리 정책 표준화

### 3단계

- mini-redis에 source backend 선택 기능 추가
- 필요하면 mini-redis 내부 cache-aside 모드 실험

즉 처음부터 mini-redis 안에 모든 정책을 몰아넣기보다,  
애플리케이션 쪽에서 먼저 사용 패턴을 검증하는 것이 더 안전합니다.

## 22. 정리

Spring이든 Flask든 연결 방법의 핵심은 같습니다.

- mini-redis를 캐시 서버로 둔다
- 앱 서비스 계층에서 먼저 캐시를 조회한다
- miss면 원본 DB를 읽는다
- 읽은 결과를 다시 mini-redis에 저장한다

이 방식이 가장 이해하기 쉽고, 운영에서도 많이 쓰는 구조입니다.

그리고 Spring에서는 이 흐름을 `MiniRedisCacheManager`를 통해 감싸면,  
개발자는 `@Cacheable`, `@CachePut`, `@CacheEvict`처럼 Redis 쓸 때와 거의 비슷한 방식으로 사용할 수 있습니다.

## 23. 한 줄 요약

Spring과 Flask에서는 mini-redis를 보통 **서비스 계층에서 사용하는 캐시 서버**로 연결하고,  
Spring에서는 custom `CacheManager`를 만들어 `@Cacheable`, `@CachePut`, `@CacheEvict`처럼 사용하는 방식이 가장 현실적입니다.
